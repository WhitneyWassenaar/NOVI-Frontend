# Frontend development
Difference between website and  application
- Website: static content (read only)
- Application: interactive content (create accounts, add data, edit (online software))

backend: webserver, invisible for user, save and retrieve data in database

## Tags and elements  
`<p>` : paragraph  
`h1`: heading  
`<i>`: italic  
`<em>`: emphasize

`<!DOCTYPE html>` Document Type Definition (DTD)

## Text formatting
`<i>`: italic, but use `<em>` emphasize instead  
`<b>`: bold, but use `<stong>` instead  
`<u>`: underline, but use it in CSS instead   
`<a>`: hyperlink
`<href>`: Hypertext reference
`<img>`: image
`<src`: source
`<alt`: alternate text

SEO: Search Engine Optimization

Commenting out text: ctrl + /


